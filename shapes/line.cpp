#include <cmath>
#include <vector>
#include "../utils/header.h"
using namespace std;

// struct  Vector2 { int x, y; };
// struct Vector2 { float x, y; };

vector<int*> AddLine(Screen* A, float(*Line)(int), int* range) {
  vector<int*> Points;
  for (int i = range[0];i <= range[1];i++) {

  }
  return Points;
}

int main() {
  Vector2 A(2, 3);
}